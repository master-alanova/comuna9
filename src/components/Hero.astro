---

---

<section class="hero">
    <div class="hero-image">
        <img src="/images/hero-fire.jpg" alt="Fuego de cocina Comuna 9" />
        <div class="hero-overlay"></div>
    </div>

    <div class="hero-content">
        <h1 class="hero-title">
            Comuna 9: Cocina de Estación en Mataderos - Comida Noble
        </h1>
        <p class="hero-subtitle">
            En el corazón de Mataderos, celebramos lo esencial: productos sin
            artificios y una mesa bien puesta
        </p>
        <a href="#reserva" class="btn btn-primary">Reservar Mesa</a>
    </div>
</section>

<style>
    .hero {
        position: relative;
        height: 100vh;
        min-height: 600px;
        display: flex;
        align-items: center;
        justify-content: center;
        overflow: hidden;
    }

    .hero-image {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 0;
    }

    .hero-image img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        filter: brightness(0.7);
    }

    .hero-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(
            to bottom,
            rgba(26, 26, 26, 0.3) 0%,
            rgba(26, 26, 26, 0.6) 100%
        );
    }

    .hero-content {
        position: relative;
        z-index: 1;
        text-align: center;
        color: var(--color-text-light);
        max-width: 900px;
        padding: 0 2rem;
        animation: fadeInUp 1s ease-out;
    }

    .hero-title {
        font-size: clamp(2.5rem, 5vw, 4rem);
        font-weight: 400;
        margin-bottom: 1.5rem;
        line-height: 1.2;
        text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
    }

    .hero-subtitle {
        font-size: clamp(1.125rem, 2vw, 1.5rem);
        font-weight: 300;
        margin-bottom: 2.5rem;
        opacity: 0.95;
        line-height: 1.6;
    }

    @media (max-width: 768px) {
        .hero {
            min-height: 500px;
        }

        .hero-content {
            padding: 0 1rem;
        }
    }
</style>
